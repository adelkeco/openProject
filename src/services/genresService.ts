import axios from 'axios';
import { GET_GENRES } from 'utils/api';

const GenresService = {
    getGenres: () => {
        //return axios.get(`${process.env.REACT_APP_API_URL}${GET_GENRES}?api_key=${process.env.REACT_APP_API_KEY}`)
        
    }
}

export default GenresService;